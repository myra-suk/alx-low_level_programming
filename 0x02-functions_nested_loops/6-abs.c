#include "main.h"

/**
 * _abs - Computes the absolte value
 * of an integer
 *
 * @x: input number which is of integer value
 *
 * Return: absolute value
 */
int _abs(int x)
{
if (x >= 0)
{
return (x);
}
else
{
return (x * -1);
}
}
